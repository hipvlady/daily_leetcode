
def longestSubStr(s:str) -> int:
    seen  = set()
    L, max_l =0,0
    n =len(s)
     
    for R in range(s):
        seen.add(s[R])
        while 
            seen.remove(s[L])
            L +=1
            max_l = (max, len(seen))

    return max

s = "abcabcbb"